(ns studio.audio)

(defn configure [spec]
  (deref (Host/call "studio.audio" "configure" [spec])))

(defn control
  ([command] (control command nil))
  ([command value]
   (deref (Host/call "studio.audio" "control" [command value]))))
